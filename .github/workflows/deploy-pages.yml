# Publicação do site: pasta docs/ na branch main
# Settings → Pages → Deploy from a branch → main → /docs
# URL: https://suellenmiranda.github.io/suellen/
name: noop

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Site publicado via pasta docs/ (não via Actions)."
